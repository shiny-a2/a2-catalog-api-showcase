# Sample Code Plan

Future sanitized samples may include:

- a generic product transformer example;
- a taxonomy term normalization helper;
- an API response DTO shape;
- a safe request validation pattern.

Samples must avoid real product data, endpoint URLs, credentials, and private consumer contracts.
